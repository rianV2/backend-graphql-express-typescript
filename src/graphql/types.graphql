scalar Timestamp

scalar Id

interface RecognizedEntity {
  id: Id!
  name: String!
  displayName: String!
  displayPicture: String!
}

type PublicFigure implements RecognizedEntity {
  id: Id!
  name: String!
  displayName: String!
  displayPicture: String!
}

type CrudStatus {
  # entities: PublicFigure!,
  status: String!
}
